{
  "name": "@dragonvite/database",
  "version": "0.1.0",
  "description": "Prisma schema and database management for Dragonvite",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "push": "prisma db push",
    "reset": "prisma migrate reset",
    "studio": "prisma studio",
    "seed": "node prisma/seed.js"
  },
  "devDependencies": {
    "prisma": "^5.7.0"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0"
  }
}
